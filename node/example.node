<?php
$link_text = '';//where the visual text goes
$div = '';//targeted div
$href = '';//where the data is going
$url = '';//data being transmitted
$loads = '';//what runs when the new content is loaded
make_link($link_text, $div, $href, $url, $loads);
